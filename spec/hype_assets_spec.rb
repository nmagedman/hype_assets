require 'spec_helper'

describe HypeAssets do
  it 'has a version number' do
    expect(HypeAssets::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
